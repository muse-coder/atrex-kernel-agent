[task]
slug = "rtxpro5000_fp8_gemm__m1024"
arch = "sm120"
target_gpu = "NVIDIA RTX PRO 5000 72GB Blackwell"
family = "fp8_gemm"
sm_count = 110

[build]
language = "cuda"
destination_passing_style = true
baseline_entry_point = "baseline/fp8_gemm_baseline.py::fp8_gemm_baseline"
candidate_entry_point = "solution/kernel.cu::fp8_gemm_candidate"

[benchmark]
warmup_runs = 10
iterations = 200
num_trials = 7
inner_iterations_min = 1
inner_iterations_max = 4096
target_sample_us = 1000
timeout_seconds = 600
use_isolated_runner = true
required_matched_ratio = 1.0

[environment]
python = "/home/moudi.mou/open_env/bin/python"
